title = "FooBar";

description = ``;

characters = [];

const G ={
   WIDTH: 150,
   HEIGHT: 150, 
};

options = {
    viewSize: {x: G.WIDTH, y:G.HEIGHT},
};

function update() {
    if (!ticks) {
    }
}

addEventListener("load", onLoad);